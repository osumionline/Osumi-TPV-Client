export default interface CaducidadesWorkspaceState {
  readonly anio: number | null;
  readonly mes: number | null;
  readonly idMarca: number | null;
  readonly nombre: string;
  readonly pagina: number;
  readonly num: number;
}
